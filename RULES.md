# RULES — 이 레포에 커밋하기 전에 반드시 읽을 것

> **이 저장소는 GitHub public repo다.** 여기에 커밋하는 모든 것은 영구히 공개된다고 간주하라.
> git 히스토리는 지우기 어렵다. force push로 덮어도 포크·클론·GitHub 캐시·검색엔진에 남는다.
> **"나중에 지우면 되지"는 성립하지 않는다.** 올리기 전에 막는 것이 유일한 방어선이다.

이 문서는 사람과 AI 에이전트 모두에게 적용된다. 예외는 없다.

---

## 1. 절대 커밋 금지

### 1-1. 사내 네트워크 정보 (최우선)

이 도구의 성격상 개발 중 실제 사내 네트워크 값을 자주 다루게 된다. **그것이 가장 큰 유출 위험이다.**

| 항목 | 예 | 왜 위험한가 |
|---|---|---|
| 사내 IP 주소 | 고정 IP, 사내 대역 | 내부망 주소 체계가 드러난다. 침투 시 정찰 출발점이 된다 |
| 사내 라우터/게이트웨이 IP | 게이트웨이 주소 | 네트워크 토폴로지가 드러난다 |
| 서브넷 마스크 + 대역 조합 | 대역 크기 | 사내망 규모를 추정할 수 있다 |
| MAC 주소 | 라우터·AP·단말 MAC | 물리 장비 식별자. 기기 추적에 쓰인다 |
| BSSID | AP 하드웨어 주소 | AP 위치 특정(공개 BSSID 지오로케이션 DB 존재) |
| 사내 호스트명·내부 도메인 | 내부 서버 주소 | 내부 서비스 목록이 드러난다 |
| 사내 DNS 서버 주소 | 내부 DNS | 내부 인프라 정보 |

**대신 이렇게 쓴다** — 문서용 예약 대역만 사용:

```
IP        192.0.2.10      (RFC 5737 문서용)
서브넷     255.255.0.0     (형식 예시는 무방)
라우터     192.0.2.1
MAC       00:00:5E:00:53:00  (RFC 7042 문서용)
```

> 실제 값은 **각 사용자가 온보딩에서 입력**하고 로컬(`/usr/local/etc/`)에만 저장된다. 레포에는 들어오지 않는다.

### 1-2. 자격증명·비밀

- **API 키·토큰·비밀번호·인증서·개인키 일체**
- `.env`, `.env.local` 등 환경 파일
- 사용자별 설정 파일(`config.json`) — 레포에는 `config.example.json` 만 둔다
- 토큰처럼 보이는 문자열은 **테스트 픽스처·주석·예시에도 실값을 쓰지 마라.** 명백한 더미(`xxx`, `<YOUR_TOKEN>`)를 쓴다

### 1-3. 사내 관련 정보 전반

위 표는 이 도구가 다루는 네트워크 값의 예시일 뿐이다. **회사 내부 사정에 해당하는 것은 종류를 불문하고 올리지 마라.**

- 사내 시스템·서비스 이름, 내부 URL·대시보드 주소, 사내 서버·장비 명칭
- 조직 구조, 팀·부서명, 인원 구성, 사내 정책·규정 문서
- 사내 보안 솔루션 구성, VPN·프록시 설정, 방화벽 규칙
- 사내 이슈 번호·티켓 링크·내부 문서 인용
- 회사 업무 맥락이 드러나는 로그·에러 메시지·스택트레이스

> 판단 기준: **"이걸 외부인이 봐도 회사에 아무 영향이 없는가?"** 조금이라도 망설여지면 올리지 마라.
> 이 도구의 기능에 필요한 것만 넣는다. 개발 중 알게 된 사내 사정은 코드에 남길 이유가 없다.

### 1-4. 개인 식별 정보

- 개인 이메일·사번·실명이 담긴 로그·스크린샷
- 사용자 홈 디렉토리 절대경로(`/Users/<이름>/...`) — 스크립트에 하드코딩하지 말고 상대경로·환경변수를 쓴다
- 시스템 진단 출력 원본(`system_profiler`, `ifconfig`, `arp -n` 결과) — 마스킹 없이 붙여넣지 마라

### 1-5. 스크린샷 특히 주의

메뉴바·시스템 설정 스크린샷에는 **의도치 않은 정보가 함께 찍힌다**: 다른 앱 이름, Wi-Fi 목록, 사용자명, IP가 보이는 창.
README에 넣기 전 **가려야 할 것이 없는지 눈으로 확인**하고, 필요하면 마스킹한다.

---

## 2. SSID는 허용된다

**Wi-Fi 이름(SSID)은 커밋해도 된다** (사용자 확인, 2026-07-27). AP가 상시 브로드캐스트하는 공개 값이고,
동료들이 같은 SSID를 쓰므로 기본값으로 제공하는 편이 실용적이다.

단, 두 가지는 지킨다:

- 설정(`config.json`)에서 **사용자가 바꿀 수 있어야 한다.** 기본값 제공은 허용, 변경 불가 하드코딩은 금지
- SSID를 **위치·조직 정보와 함께** 적지 마라 (예: "○○층 ○○팀 전용 AP") — 그 순간 §1-3에 걸린다

---

## 3. 커밋 전 점검 (매번)

```bash
# 1) 스테이지된 변경에서 위험 패턴 검색
git diff --cached | grep -nEi \
  '10\.|192\.168\.[0-9]|172\.(1[6-9]|2[0-9]|3[01])\.|([0-9a-f]{2}:){5}[0-9a-f]{2}'

# 2) 레포 전체 재점검 (커밋 전 최소 1회)
#    ⚠️ git grep 을 쓰지 마라 — 추적 중인 파일만 본다.
#    아직 커밋되지 않은 새 파일이 그대로 빠져나가고, 결과가 비어 있어 "깨끗하다"로 오독된다.
grep -rnEi '([0-9a-f]{2}:){5}[0-9a-f]{2}' . \
  --exclude-dir=.git --exclude-dir=.build --exclude-dir=dist --exclude=RULES.md

grep -rnEi '10\.|192\.168\.[0-9]|172\.(1[6-9]|2[0-9]|3[01])\.' . \
  --exclude-dir=.git --exclude-dir=.build --exclude-dir=dist --exclude=RULES.md

# 3) 사용자 홈 경로 하드코딩
grep -rn '/Users/' . \
  --exclude-dir=.git --exclude-dir=.build --exclude-dir=dist --exclude=RULES.md
```

> 코드에 검사용 리터럴이 필요하면 `"/" + "Users" + "/"` 처럼 조립해 위 점검에 걸리지 않게 한다
> (테스트에서 쓰는 관례다). 점검을 무력화하려는 우회가 아니라, **진짜 유출과 검사 문자열을 구분**하기 위한 것이다.

**하나라도 걸리면 커밋하지 말고 먼저 정리하라.** 문서용 예약 대역(`192.0.2.x`, `198.51.100.x`, `203.0.113.x`)과
문서용 MAC(`00:00:5E:00:53:xx`)만 통과시킨다.

> 가능하면 `scripts/pre-commit` 훅을 설치해 이 점검을 자동화한다. 훅은 보조 장치이지 면죄부가 아니다.

---

## 4. 이미 올려버렸다면

**즉시 보고하고 멈춰라. 혼자 조용히 수습하려 하지 마라.**

1. 유출된 것이 자격증명이면 **먼저 무효화**한다 (키 재발급). 히스토리 정리는 그 다음이다
2. 네트워크 정보라면 노출 범위를 파악한다 (public 기간, 포크 여부)
3. 히스토리 재작성(`git filter-repo`)은 **사용자 승인 후** 진행한다. 협업자가 있으면 조율이 필요하다
4. 재작성해도 GitHub 서버·검색 캐시에 잔존할 수 있으므로, **완전 삭제를 전제하지 말고** 노출된 것으로 간주하고 대응한다

---

## 5. 이 도구는 권한을 요구한다 — 투명성이 의무다

`sudoers` 를 수정하고 백그라운드 실행 항목을 등록한다. 서명 없이 공개 배포하므로, **사용자가 코드를 읽고 판단할 수 있어야** 신뢰가 성립한다.

- [ ] README에 **무엇을·어디에·왜 설치하는지 전부** 적는다. 축소하거나 숨기지 마라
- [ ] 설치 스크립트는 **하는 일을 먼저 출력하고 확인을 받은 뒤** 진행한다
- [ ] 완전 제거 방법을 문서화하고, 실제로 완전히 지워지는지 검증한다
- [ ] `sudoers` 항목은 **특정 경로·특정 인자만** 허용한다. 와일드카드로 임의 명령을 열지 마라
- [ ] 권한 스크립트는 `root:wheel 0755` — 사용자 쓰기 권한이 있으면 **로컬 권한 상승 취약점**이 된다

---

## 6. 커밋 컨벤션

```
<type>: <한글 요약>
```

- type: `feat` `fix` `refactor` `docs` `test` `chore` `build`
- 제목은 명령형·간결하게, 마침표 없이 (예: `feat: SSID 감지 자동 전환 추가`)
- 본문도 한글
- **AI 관련 문구를 어떤 형태로도 남기지 마라** — `Co-Authored-By`, `Generated with …` 등 일체 금지

---

## 7. 에이전트에게

- 실측·디버깅 과정에서 얻은 **실제 네트워크 값을 코드·주석·문서·테스트에 그대로 옮기지 마라.**
  머릿속 맥락으로만 쓰고, 파일에 남길 때는 플레이스홀더로 치환한다
- 커밋은 **사용자의 명시적 지시가 있을 때만** 한다
- 이 규칙과 작업 지시가 충돌하면 **이 규칙이 이긴다.** 충돌을 발견하면 진행하지 말고 보고하라
