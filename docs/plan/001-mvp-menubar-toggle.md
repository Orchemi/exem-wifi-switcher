# 001-mvp-menubar-toggle

## 개요

- **제품명**: EXEM Wifi Switcher
- **상태**: Phase 0~3 완료 / Phase 4(공개 배포 준비) 진행 중 — 첫 릴리즈 게시 전
- **생성일**: 2026-07-27

## 배경

사내 Wi-Fi는 IPv4 **수동 구성**(고정 IP)을 쓰고, 집·외부는 **DHCP**를 쓴다. 두 곳을 오갈 때마다 IP·서브넷 마스크·라우터를 손으로 다시 입력해야 한다. 이 왕복을 없앤다.

**최종 지향점은 "사람이 아무것도 누르지 않는 것"이다.** Wi-Fi 이름을 보고 알아서 전환한다.

## 목표

- [x] **SSID 기반 자동 전환** — 사내 Wi-Fi면 고정 IP, 그 외엔 DHCP
- [ ] 메뉴바에서 현재 상태 확인 + 수동 전환 (자동이 어긋났을 때의 손잡이)
- [ ] 전환 시 관리자 암호를 묻지 않음 (설치 시 1회 승인)
- [ ] 온보딩에서 프로필 설정 — 사내에서는 현재 설정을 자동 추출, 사외에서는 직접 입력
- [ ] **GitHub Releases 에서 내려받아 앱 안에서 설치** — 동료가 터미널을 한 번도 열지 않아도 된다
- [ ] 로그인 항목에 `EXEM Wifi Switcher` 로 표시

### 비목표

- **서명·공증** — 유료 Developer Program 가입 의사 없음(사용자 확인). 그래서 내려받은 앱은
  첫 실행에서 Gatekeeper 에 한 번 막힌다. 그 사실과 여는 방법을 README 에 그대로 적는다.
  경고를 없앨 방법이 없으므로, 대신 **읽을 수 있는 코드 + SHA-256 + 직접 빌드**를 확인 수단으로 둔다.
- **`.pkg` 인스톨러** — 설치 시점에 시스템을 바꾸는 방식을 쓰지 않는다. 앱을 먼저 두고,
  **앱 안에서 사용자가 계획을 보고 누를 때** 설치한다 (RULES.md §6 의 "먼저 보여주고 확인을 받는다").
- **Homebrew cask 배포** — 이번 범위 밖. 동료 몇 명이 쓰는 도구에 배포 채널이 둘일 이유가 없다.
- **데스크톱 위젯** — 철회됨. 자동 전환이 되면 누를 일이 없다.
  (참고: Phase 0에서 위젯 경로가 기술적으로 가능함은 확인됐다. 제품 판단으로 접은 것이다.)

> **"소스 공개 + 각자 빌드"는 배포 방식이 아니라 검증 수단으로 물러났다** (2026-07-28).
> 빌드된 앱을 Releases 에 올린다. 소스는 그대로 공개하고 직접 빌드하는 길도 남긴다.

## 공개 배포 원칙 (public repo 전제 — 전 Phase에 적용)

이 레포는 **공개된다.** 아래는 협상 대상이 아니다.

- [ ] **실제 사내 네트워크 값을 레포에 커밋하지 마라.** IP·서브넷·라우터·MAC 주소는 사내 인프라 정보다.
      README·예시·테스트 픽스처는 전부 플레이스홀더(`10.0.0.x`, `192.168.x.x` 등 문서용 예시)를 쓴다.
      실제 값은 **각 사용자가 온보딩에서 입력**하고 로컬에만 저장된다.
- [ ] **사용자 설정 파일은 커밋되지 않는다** — `config.json`은 `/usr/local/etc/` 에 있고 레포에는 `config.example.json` 만 둔다.
- [ ] **투명성 문서가 필수다.** 이 도구는 `sudoers`를 수정하고 백그라운드 실행 항목을 등록한다.
      README에 **무엇을·어디에·왜 설치하는지, 어떻게 완전히 제거하는지**를 숨김없이 적는다.
      권한을 요구하는 도구를 공개 배포하면서 그 내역을 감추면 안 된다.
- [ ] 설치 스크립트는 **하는 일을 먼저 출력하고 사용자 확인을 받은 뒤** 진행한다. 조용히 시스템을 바꾸지 마라.

## 설계

### 제약 조건

1. `networksetup -setmanual/-setdhcp` 는 **root 권한**이 필요하다.
2. SSID 조회는 **위치 권한 + `.app` 번들**이 필요하다 (Phase 0에서 실증, 아래 참조).
3. **코드서명 인증서 없음** (무료 Apple ID). ad-hoc 서명으로 간다.
   ~~`SMAppService`(Developer ID 요구)는 쓸 수 없다.~~ → **틀렸다.** `SMAppService.mainApp` 은
   ad-hoc 서명으로 동작한다 (2026-07-29 실측 ↓). Developer ID 를 요구하는 것은 `.daemon` 쪽이다.
4. **Xcode 16.4 = macOS 15.5 SDK.** macOS 26 전용 API는 쓸 수 없다.

### Phase 0 실증 결과 (확정 — 재조사 불필요)

| 항목 | 결과 |
|---|---|
| SSID 조회 방법 | **CoreWLAN `CWInterface.ssid()` 만 동작.** `ipconfig getsummary`·`system_profiler` 는 `<redacted>` 반환 |
| 전제 조건 | **`.app` 번들 + `NSLocationWhenInUseUsageDescription` + ad-hoc 서명.** 순수 CLI 바이너리는 TCC 귀속이 안 돼 영구 실패 |
| 권한 지속성 | ~~번들 ID 단위로 유지. **재빌드·재서명해도 재승인 불필요**~~ → **틀렸다. 다시 빌드하면 위치 권한이 풀린다** (2026-07-29 실기 확인 ↓) |
| 권한 범위 | 해당 앱에만 적용. 셸의 `ipconfig` 는 승인 후에도 여전히 가려짐 |
| root 컨텍스트 | 미검증이나 **불필요** — 앱이 SSID를 읽어 전달하므로 root는 알 필요가 없다 |
| 위치 권한 없는 대안 | 라우터 IP / 라우터 MAC(ARP) 는 권한 없이 조회 가능. **BSSID는 위치 권한 필요** |

> **설계적 함의**: SSID 판독은 앱(사용자 세션) 책임, 네트워크 변경은 root 책임으로 나뉜다.

#### 정정 — 다시 빌드하면 위치 권한이 풀린다 (2026-07-29 실기 확인)

Phase 0 은 "권한은 번들 ID 단위로 유지된다" 로 적었다. **틀렸다.** 새 빌드로 앱을 바꾸면
위치 권한이 사라지고 승인 창을 다시 받아야 한다. 앱을 쓰는 사람에게 실제로 일어난 일이다.

**왜** — ad-hoc 서명(`codesign --sign -`)에는 **고정된 신원이 없다.** 유료 인증서라면 Team ID 로
같은 앱임을 증명하지만, ad-hoc 은 그럴 근거가 없어 TCC 가 **코드 해시**로 앱을 가른다.
코드가 한 줄만 바뀌어도 해시가 달라지고, macOS 는 그것을 **이름만 같은 다른 앱**으로 본다.

**무엇이 남고 무엇이 사라지는가** — 원인이 같아도 결과가 갈린다. 이 구분이 요점이다.

| 대상 | 무엇에 매여 있는가 | 재빌드 후 |
|---|---|---|
| 위치 권한 | 코드 해시(ad-hoc 서명) | **풀린다** |
| 알림 권한 | 번들 식별자 | 남는다 |
| 전환 권한(`apply`·sudoers)·설정 파일 | 앱 밖 (`/usr/local/…`) | 남는다 |
| 로그인 항목 | 번들 식별자 (BTM 기록) | 남는다. ~~macOS 가 "백그라운드에서 실행될 수 있습니다" 알림을 한 번 띄운다~~ → **옛 방식(LaunchAgent) 이야기다.** `SMAppService` 로 옮긴 뒤로는 항목 종류가 `agent` 가 아니라 `app` 이라 그 알림이 뜨지 않는다 (2026-07-29 실측: BTM 이 `disposition=[enabled, allowed, not notified]` 로 기록한다) |

**고칠 수 있는가** — 없다. 서명·공증은 비목표이므로(위 [비목표](#비목표)) 이 성질은 남는다.
할 수 있는 것은 **놀라지 않게 적어 두는 것**이다: README `업데이트할 때 생기는 일` ·
`scripts/build-app.sh` 머리말과 완료 안내. 동료가 업데이트한 뒤 권한이 풀리고 낯선 알림을 보면
앱을 의심하게 된다 — 미리 알고 있으면 승인 창 한 번으로 끝나는 일이다.

### 접근 방식

```
[EXEM Wifi Switcher.app]  ← 메뉴바 앱 (로그인 시 자동 실행)
   ├ CoreLocation 1회 승인 → CWInterface.ssid() 로 현재 SSID 판독
   ├ SCDynamicStore 로 네트워크 변경 감시
   ├ 판정: SSID가 사내 목록에 있으면 → 고정 IP 프로필 / 아니면 → DHCP
   └ 적용: sudo -n /usr/local/libexec/exem-wifi-switcher/apply <프로필>
                          ↓
   [apply 스크립트 · root:wheel 0755] → networksetup -setmanual / -setdhcp
        ↑ /etc/sudoers.d/exem-wifi-switcher 가 이 경로만 NOPASSWD 로 허용
```

root 헬퍼 데몬과 XPC가 없다. 위젯 샌드박스가 빠지면서 `sudo -n` + sudoers 화이트리스트만으로 충분해졌다.

### 빌드: SPM + 번들 조립 (Xcode 프로젝트 아님)

**동료가 Xcode 없이 Command Line Tools만으로 빌드할 수 있어야 한다.** 공개 배포의 진입장벽을 좌우한다.

- Swift Package Manager로 실행 파일 빌드 → 스크립트로 `.app` 번들 조립(Info.plist·아이콘·ad-hoc 서명)
- Xcode·XcodeGen 의존 제거 (Phase 0의 `project.yml`·`Widget/`·`Helper/`·XPC 코드는 **폐기**)
- `Info.plist`에 **`NSLocationWhenInUseUsageDescription` 필수** — 없으면 SSID 경로가 통째로 막힌다

### 네이밍 (고정)

| 대상 | 값 |
|---|---|
| 제품 표시명 | `EXEM Wifi Switcher` |
| 앱 번들 | `EXEM Wifi Switcher.app` |
| Bundle ID | `com.horbis.exem-wifi-switcher` |
| LaunchAgent Label | `com.horbis.exem-wifi-switcher.agent` |
| GitHub | `Orchemi/exem-wifi-switcher` (public) |
| 로컬 디렉토리 | `tools/exem-wifi-switcher` |
| 권한 스크립트 (전환) | `/usr/local/libexec/exem-wifi-switcher/apply` — sudoers 로 무암호 |
| 권한 스크립트 (저장) | `/usr/local/libexec/exem-wifi-switcher/save-config` — **관리자 인증 필요, sudoers 에 없음** |
| sudoers 파일 | `/etc/sudoers.d/exem-wifi-switcher` |
| 설정 파일 | `/usr/local/etc/exem-wifi-switcher/config.json` (`root:wheel 0644`) |

> **반드시 `.app` 번들로 등록하라.** 맨 실행 파일을 LaunchAgent로 등록하면 로그인 항목에 실행 파일 이름이 그대로 노출된다
> (Phase 0에서 `server` 로 표시되는 사고가 실제로 발생했다). 서명이 없어 "확인되지 않은 개발자" 표기는 남지만, 이름만은 제품명으로 보여야 한다.

### 보안 요구사항 (타협 불가)

sudoers NOPASSWD 설계이므로 아래를 어기면 **로컬 권한 상승 취약점**이 된다. 공개 배포되므로 더욱 엄격히 지킨다.

- [ ] `apply` 스크립트는 **`root:wheel`, `0755`** — 사용자 쓰기 권한이 있으면 안 된다
- [ ] 상위 디렉토리도 root 소유 (심링크 교체 공격 차단)
- [ ] sudoers 항목은 **전체 경로 + 인자 패턴 제한**. 와일드카드로 임의 명령을 열지 마라
- [ ] `apply`는 인자를 화이트리스트 검증 (프로필 이름만 허용, 셸 인젝션 차단)
- [ ] **설정 파일은 `root:wheel 0644`, 설정 디렉터리는 `root:wheel 0755`.**
      이 파일의 값이 root 의 `networksetup` 인자가 되므로, 사용자 권한으로 고칠 수 있으면
      암호 없는 네트워크 재구성이 된다 (위 "뒤집힌 결정" 참조)
- [ ] **저장 경로(`save-config`)를 sudoers 에 넣지 마라.** 넣는 순간 위 잠금이 무의미해진다
- [ ] 상위 디렉터리를 하나씩 명시적으로 만든다. BSD `install -d` 는 중간 디렉터리도 같은
      소유·모드로 만들어, 이 도구와 무관한 시스템 디렉터리(`/usr/local/etc`)를 잘못된 권한으로 남긴다
- [ ] 설치 시 sudoers를 임시 경로에 쓰고 **`visudo -c` 검증 후** 이동.
      검증 없이 잘못된 파일을 놓으면 **`sudo` 자체가 망가진다**

### 디자인 방향 (오너 지시 — 아이콘·UI 전반)

> "깔끔하고 전문적이면서도 미적인 것. **담백하게.**"

- **판단이 갈리면 덜어내는 쪽으로.** 요소를 하나 빼서 나빠지지 않으면 빼는 게 맞다
- "미적"은 화려함이 아니라 **비례·정렬·여백의 정확함**이다. 장식을 더하지 말고 정렬에 공을 들여라
- 색은 최소한으로, 상태 구분에 꼭 필요한 곳에만, 채도는 낮게
- 귀엽거나 소비자 앱처럼 튀는 방향이 아니다 — **실무자가 메뉴바에 상시 띄워두는 전문 도구**의 얼굴
- macOS 시스템 앱의 절제된 톤을 기준점으로 삼는다

### 커밋 컨벤션

- 형식: `<type>: <한글 요약>` (예: `feat: SSID 감지 자동 전환 추가`)
- type: `feat` / `fix` / `refactor` / `docs` / `test` / `chore` / `build`
- 본문도 한글. **AI 관련 문구(`Co-Authored-By`, `Generated with …`)를 어떤 형태로도 남기지 마라**
- 제목은 명령형·간결하게, 마침표 없이

### 의사결정 기록

| 결정 사항 | 선택지 | 결정 | 이유 |
|---|---|---|---|
| UI 형태 | 위젯 / 메뉴바 앱 | **메뉴바 앱** | 자동 전환이면 누를 일이 없어 위젯 가치가 낮고 샌드박스 복잡도만 남음 |
| 권한 경로 | root 헬퍼+XPC / sudoers | **sudoers NOPASSWD** | 샌드박스 제약 소멸로 XPC 불필요. 코드량 대폭 감소 |
| 빌드 | Xcode 프로젝트 / SPM | **SPM + 번들 조립** | 동료가 Xcode 없이 CLT만으로 빌드 가능 → 배포 진입장벽 최소화 |
| 배포 | 바이너리(dmg) / 소스 공개 | ~~**소스 공개**~~ (2026-07-28 뒤집힘 ↓) | 서명 불가 상태에서 root 권한 도구를 배포하려면 코드가 감사 가능해야 한다 |
| 설치 방식 | 스크립트 / `.pkg` / **앱 안에서 버튼** | **앱 안에서 버튼** (2026-07-28) | 터미널을 여는 순간 대부분 포기한다. `.pkg` 는 설치 시점에 시스템을 바꿔 "먼저 보여주고 확인을 받는다" 를 지키기 어렵다. 앱이 계획을 띄우고 사용자가 누를 때 설치한다. **설치 로직은 `install.sh` 한 곳에 그대로 두고** 앱이 그 파일을 번들에 품어 실행한다 |
| 배포 형태 | Releases 바이너리 / 소스만 / Homebrew | **Releases 바이너리** (2026-07-28) | 소스만 두면 각자 CLT 설치·clone·빌드를 해야 한다 — 설치를 버튼으로 만든 의미가 사라진다. Homebrew 는 사용자가 몇 명인 도구에 과하다. 소스는 그대로 공개해 검증 수단으로 남긴다 |
| SSID 판독 위치 | 앱 / root | **앱** | Phase 0 실증: 위치 권한은 `.app` 번들에만 귀속. root는 알 필요 없음 |
| 자동 전환 기본값 | on / off | **on** | 목표가 "사람이 건들 것 없게". 단 전환 시 알림으로 가시성 확보 |
| 설정 파일 권한 | `root:admin 0664` / `root:wheel 0644` | **`root:wheel 0644`** (2026-07-27 뒤집힘) | 아래 "뒤집힌 결정" 참조 |
| 프로필 이름 길이 | 16자 / 확장 | **16자 유지** | `office`·`home` 수준이면 충분. 늘리면 sudoers 고정 패턴 줄 수가 그대로 늘어난다 |
| DNS | IPv4만 / IPv6 포함 | **IPv4만** | 범위 확장의 실익이 없다. 한계를 README에 명시한다 |
| 메뉴의 빈 자리 | 자리표시자 / 무리째 감춤 | **무리째 감춤** (2026-07-28) | '등록된 프로필 없음' 은 머리말이 이미 말한 상태를 한 번 더 말하는 줄이었다. **할 수 있는 것이 없으면 자리도 차지하지 않는다** — 초기 설정 전에는 프로필 목록도 자동 전환 토글도 서지 않고 [초기 설정하기 / 설정… / 종료] 만 남는다. 구분선은 무리 **사이에만** 넣어(`MenuLayout`) 겹치거나 끝에 남을 수 없게 했다. 다만 **자동 전환이 실제로 돌고 있는 상태**(저장 권한만 없음 · Wi-Fi 이름만 없음)에서는 토글을 감추지 않는다 — 끄지도 못하는 채로 도는 것이 더 나쁘다 |
| 메뉴바 UI | NSMenu / 커스텀 팝오버 | **NSMenu** | macOS 시스템 앱의 절제된 톤에 부합. 오너 지시("담백하게")와 한 방향이고, 커스텀 UI는 장식이 붙기 쉽다 |
| 설정 저장 권한 경로 | sudoers 확장 / 관리자 인증 / 터미널 안내 | **관리자 인증 1회** (`save-config`) | 저장은 온보딩 1회성이라 인증을 받아도 UX 손해가 작다. sudoers 에 저장 경로를 넣으면 파일을 잠근 의미가 그대로 사라진다 |
| 초기 설정 완료 기준 | 설정 파일만 / 권한까지 포함 | **권한까지 포함** (2026-07-28) | 값을 다 채워도 권한이 없으면 아무것도 되지 않는다. 앱에게는 다른 일이지만 사용자에게는 똑같이 "아직 안 끝난 것" 이라 머리말('초기 설정하기') 하나로 묶는다 |
| 위치 권한 | 선택 / 필수 | ~~**선택**~~ → **필수** (2026-07-28 뒤집힘 ↓) | 아래 "뒤집힌 결정" 참조 |
| 네트워크 서비스 선택 | 항상 보임 / 감춤 | **감춤** (2026-07-28) | 이 도구는 Wi-Fi 이름으로 판단하므로 Wi-Fi 전용이다 — 고를 이유가 없는 선택지를 화면에 두지 않는다. 다만 그 서비스를 못 찾은 기기에서는 손쓸 길이 사라지므로 **그때만 드러낸다** (`config.json` 의 `service` 필드는 그대로) |
| 등록하는 사내 Wi-Fi 수 | 여럿 / 하나 | **하나** (2026-07-28, 오너 판단) | 게스트망은 **개방망이라 고정 IP 를 쓸 이유가 없다.** 고정 IP 가 필요한 망 하나만 등록하면 되므로 화면에서 여럿을 다룰 자리를 없앤다. **`config.json` 의 `ssids` 는 배열 그대로 둔다** — 나중에 다시 필요해질 때 설정 파일 형식을 깨지 않으려고 구조만 남긴다 |
| Wi-Fi 이름 칸 | 자유 편집 / 잠금 / 잠금+해제 버튼 | **사내에서만 잠금** (2026-07-28) | 앱이 읽어 넣은 이름이라 손댈 이유가 없고, 실수로 고치면 자동 전환만 조용히 걸리지 않는다. 잠금 해제 버튼은 두지 않는다 — 자동으로 들어간 이름에 오타가 있을 수 없다. 대신 **사외(DHCP)에서는 잠그지 않아** 처음 넣는 길과 되돌리는 길을 남긴다 (사내인데 칸이 빈 경우 = 위치 권한 없음 도 같은 이유로 잠그지 않는다) |

### 뒤집힌 결정

#### 설정 파일 권한: `root:admin 0664` → `root:wheel 0644` (2026-07-27, 오너 판단)

**원래 기록** — "admin은 어차피 `apply`를 무암호로 부를 수 있어 **실질적 권한 상승이 없다**.
`apply`가 설정 값을 재검증하므로 임의 명령으로 이어지지 않는다. 반면 0644면 온보딩 저장마다
sudo를 물어야 해 UX 손해가 크다."

**왜 뒤집혔는가 — 비교 대상을 잘못 잡았다.**

원래 논증은 "admin이 `apply`를 부를 수 있다"를 기준선으로 삼았다. 그러나 권한 상승을 재는 기준선은
**이 도구를 설치하기 전의 시스템**이다. 설치 전에는 `networksetup -setdnsservers` 와 `-setmanual`
둘 다 관리자 인증을 요구했다. 설치 후에는 요구하지 않는다. **그 델타가 로컬 권한 상승이다.**

구체적으로, `config.json` 이 사용자 쓰기 가능하면 — **암호를 모르는 채 그 사용자로 실행되는
코드**(내려받은 앱·스크립트·악성코드)가 —

- `dns` 를 공격자 resolver 로 바꿔 root 가 시스템 DNS 를 고정하게 만든다 → 모든 이름 해석 가로채기
- `router` 를 LAN 안 공격자 호스트로 바꾼다 → 기본 게이트웨이 탈취

`apply` 의 재검증은 **형식만** 본다. 형식이 맞는 임의의 IPv4 는 그대로 통과한다.
"임의 명령으로 이어지지 않는다"는 맞지만, 임의 명령이 필요 없다는 것이 요점이다.

**바뀐 것**

- 설정 파일 `root:wheel 0644`, 설정 디렉터리 `root:wheel 0755` (예전 `root:admin 0775` 는
  admin 그룹이 파일을 갈아 끼울 수 있었다). `install.sh` 는 기존 설치의 권한도 맞춰 준다
- 저장은 `save-config` (root:wheel 0755)가 **관리자 인증을 한 번 받고** 처리한다.
  이 경로는 **sudoers 에 넣지 않는다** — 넣으면 잠금이 원위치다
- 전환(`apply`)은 그대로 무암호다. 이 도구의 존재 이유가 거기에 있다
- 비-admin 계정에서는 저장이 불가능하다는 안내는 그대로 유효하다 (인증 창을 띄우기 전에 알린다)

**UX 손해에 대한 원래 우려는 유효하지 않았다.** 저장은 온보딩과 값 변경 때뿐이고, 전환은 인증을
요구하지 않는다. "온보딩 저장마다 sudo" 는 실질적으로 "설치 후 한 번" 이다.

#### 배포: 소스 공개 + 각자 빌드 → Releases 바이너리 + 앱 안에서 설치 (2026-07-28, 오너 판단)

**원래 기록** — "서명 불가 상태에서 root 권한 도구를 배포하려면 코드가 감사 가능해야 한다.
신뢰의 근거를 서명이 아니라 읽을 수 있는 코드에 둔다."

**왜 뒤집혔는가 — 감사 가능성과 배포 방식을 하나로 묶었다.**

오너의 지적: *"설치 방식이 스크립트라면 좀 어려운데? 주변에 공유해도 거의 못 쓰겠어."*

원래 방식은 동료에게 이만큼을 요구했다 — Command Line Tools 설치 → `git clone` → `build-app.sh`
실행 → `install.sh` 실행. **터미널을 여는 순간 대부분 포기한다.** 쓰이지 않는 도구는 감사할 사람도 없다.

그런데 "코드를 읽을 수 있게 한다"와 "바이너리를 준다"는 서로 배타적이지 않았다. 소스는 그대로 공개하고,
빌드된 앱도 함께 올리면 된다. 읽고 싶은 사람은 읽고 직접 빌드하면 되고, 그렇지 않은 사람도 쓸 수 있다.

**바뀐 것**

- **배포**: GitHub Releases 에 zip. `scripts/package-release.sh` 가 묶고 SHA-256 을 찍는다
- **설치**: 앱 설정 창의 [설치] 버튼. 무엇을 설치할지(= `install.sh --dry-run` 출력) 먼저 보여주고
  확인을 받은 뒤 관리자 인증을 한 번 받는다. [제거] 도 같은 방식이다
- **설치 로직은 그대로 `scripts/install.sh` 한 곳**이다. 앱이 그 파일을 번들에 품고(`Contents/Resources/scripts/`)
  실행한다. Swift 로 다시 구현하면 두 경로가 다른 시스템을 만든다
- 서명·공증은 여전히 비목표다. 대신 Gatekeeper 경고가 왜 뜨는지와 여는 방법을 README 에 적었다

**감사 가능성은 오히려 늘었다.** 앱이 실행하는 파일이 저장소에 그대로 있고, 번들 안 사본은 서명에
봉인돼 있어 손대면 `codesign --verify` 가 잡는다. (ad-hoc 서명은 다시 만들 수 있으므로 이것을
신뢰의 근거로 삼지는 않는다 — 손댄 흔적을 잡는 장치다)

#### 위치 권한: 선택 → 초기 설정의 필수 항목 (2026-07-28, 오너 판단)

**원래 기록** — "없어도 **수동 전환은 그대로 된다.** 기능이 아예 성립하지 않는 권한(전환·저장)과
같은 칸에 두면 안 된다. 무엇보다 알림처럼 **뜻해서 거부한 사용자**에게 '초기 설정하기' 가 영영
떠 있으면 그것은 잘못된 신호다."

**왜 뒤집혔는가 — 거부한 뒤에 남는 일을 세지 않았다.**

오너의 지적: *"위치 권한을 거부하면 저기서 **사내 Wi-Fi 이름도 직접 추가해야 하잖아?**
여기서부터 유저의 사용 문턱이 몹시 높아져."*

원래 논증은 "권한이 없어도 쓸 수는 있다" 까지만 봤다. 그런데 **그 다음에 무엇을 해야 하는지**를
보면 이야기가 달라진다. 위치 권한이 없으면 앱은 지금 붙어 있는 Wi-Fi 이름을 읽지 못하고,
그러면 사용자가 사내 Wi-Fi 이름을 **손으로 적어 넣어야** 한다. 이름을 정확히 알아야 하고,
오타 하나면 자동 전환은 조용히 걸리지 않는다 — 가장 알아채기 어려운 고장이다.

이 도구의 목표는 "사람이 아무것도 누르지 않는 것" 이다. 그 목표를 내건 앱이 **시작부터
사람에게 받아쓰기를 시키는 것**은 앞뒤가 맞지 않는다. 알림과의 비교도 성립하지 않았다 —
알림은 없어도 하는 일이 같지만, 위치 권한은 **이 앱의 존재 이유인 자동 전환 자체**를 세운다.

**바뀐 것**

- 위치 권한이 `SetupChecklist` 의 필수 항목이다. 없으면 메뉴 머리말이 '초기 설정하기' 로 남는다
  (`- 위치 권한 미승인` · `- 위치 권한 없음`)
- **알림 권한은 선택 그대로다.** 없어도 전환은 되고, 뜻해서 끈 사용자에게 미완으로 남으면 안 된다.
  관리자 계정 여부도 제외 그대로다 — 설치로 해결되지 않는 것을 할 일로 적을 수는 없다
- **필수가 기능을 잠그지는 않는다.** 권한을 끝내 거부해도 메뉴에서 프로필을 골라 전환하는 것은
  그대로 된다. 체크리스트에 남아 계속 안내할 뿐이다 — 자동 전환을 포기하고 손으로 쓰겠다는
  사용자의 길까지 막을 이유는 없다
- **묻는 순서를 바꿨다.** 첫 실행에서 설정 창을 열기 **전에** 승인 창을 띄운다. 권한이 있어야
  Wi-Fi 이름이 읽히고, 그래야 온보딩의 '사내 Wi-Fi 이름' 칸이 채워진 채로 열린다
- 아직 묻지 않은 상태의 조치는 **시스템 설정이 아니라 승인 창**이다(`PermissionRemedy.requestLocationPermission`).
  창 하나면 끝날 일을 목록에서 우리 줄을 찾게 만들 이유가 없다
- 설정 창의 'Wi-Fi 이름' 칸 옆에 그 버튼을 두었다. 예전에는 이 자리에 "권한이 없어 읽지 못했습니다…
  직접 입력해도 됩니다" 라는 안내가 있었는데, **필수가 된 지금 그 문장은 틀린 길**이다

#### 수동 선택 우선 → 자동 전환이 켜져 있으면 프로필을 잠근다 (2026-07-29, 오너 판단)

**원래 기록** — "사용자가 메뉴에서 고른 프로필은 그 Wi-Fi 에 머무는 동안 자동 전환이 되돌리지 않는다.
자동이 사용자의 손을 곧바로 되돌리면 도구가 아니라 방해가 된다."

**왜 뒤집혔는가 — 자동이 켜진 채로 손으로 고르는 일 자체를 없앴다.**

오너의 지시: *"자동 전환이 켜져 있으면 저 2개 옵션이 **보이되 disabled 처리**가 되면 좋겠어.
어차피 자동 전환이 자동으로 해줄 테니까."*

자동이 곧 되돌릴 선택을 눌리게 두는 것보다, **누를 수 없게 하고 그 이유를 상태로 보여주는 편**이
정직하다. 그렇게 하면 '수동 선택 우선' 이 발동할 경로가 사라진다 — 자동이 꺼진 상태에서 고른 것은
애초에 자동이 되돌리지 않기 때문이다.

**도달 불가를 코드로 확인했다.** `recordManualChoice` 를 부르는 자리는 `apply(userInitiated: true)`
하나이고, 그 경로는 메뉴 항목이 눌려야 열린다(`item.isEnabled = model.canSwitch`). 잠긴 뒤로는
자동이 꺼져 있을 때만 눌리고, 자동을 다시 켜는 `toggleAutoSwitch` 는 그때마다 기록을 거둔다.
`AutoSwitchPolicy.decide` 는 켜져 있을 때만 그 기록을 보므로 **참조되는 순간이 오지 않는다.**
(전환이 끝나기를 기다리는 사이에 토글을 켜는 아주 좁은 겹침만 남았는데, 그때 남는 기록은
"자동에 맡기겠다" 는 방금의 선택과 어긋난다 — 살릴 값이 아니다.)

**바뀐 것**

- 자동 전환이 **일하고 있는 동안** 프로필 항목이 잠긴다. **감추지 않는다** — 항목과 체크 표시는
  그대로 선다. 머리말을 지운 뒤로 **체크 표시가 '지금 무엇이 서 있는가' 를 지고 있는 유일한 자리**다
- 잠금 사유는 **더해진다.** 자동을 꺼도 전환 권한이 없거나 전환이 진행 중이면 잠긴 채다
- **켜짐만으로 잠그지 않는다.** 자동 전환이 손을 쓸 수 없는 상태(위치 권한 없음 등)에서는 토글이
  메뉴에 서지 않으므로, 거기서 프로필까지 잠그면 **끄지도 못하고 고르지도 못하는 자리**가 된다.
  판정을 한 자리에서 나눠 쓴다 (`MenuLayout.autoSwitchCanAct`)
- `manualChoice` · `recordManualChoice` · `clearManualChoice` · `AutoSwitchHold.manualOverride` ·
  그 문구와 테스트를 **걷어냈다.** 남겨 두면 다음 사람이 살아 있는 기능으로 읽는다
- **`canSwitch` 의 뜻이 좁아졌다** — '전환이 가능한 상태' 가 아니라 '지금 프로필을 눌러 고를 수 있는가'.
  그래서 '전환 중인가' 를 묻던 자리(`MenuLayout.isToldByCheckmark`)는 `isSwitching` 을 따로 본다.
  묶어 두면 자동 전환이 켜진 정상 상태에서 **지워 둔 머리말이 통째로 되살아난다**
- 자동이 틀렸을 때 손으로 덮는 길은 **자동 전환 끄기 → 고르기** 두 단계다. 한 단계 늘지만
  무엇을 하고 있는지가 화면에 남는다 (README `자동 전환` 절에 적었다)

## 작업 단계

### Phase 0: SSID 판독 실증 ✅ 완료

- [x] SSID 조회 방법 조사 및 실측 (CoreWLAN만 동작 확인)
- [x] 위치 권한 필요 조건 확인 (`.app` 번들 + Info.plist 키 + ad-hoc 서명)
- [x] 권한 지속성 확인 — ~~재빌드해도 유지~~ **재빌드하면 위치 권한이 풀린다** (2026-07-29 정정)
- [x] 대안 식별자 조사 (라우터 IP·MAC은 권한 불필요, BSSID는 권한 필요)

### Phase 1: 전환 코어 ✅ 완료 (설치는 미실행 — 사용자가 직접)

- [x] Phase 0 잔재 정리 — `project.yml`·`Widget/`·`Helper/`·`Shared/`·`App/`·XPC 코드 폐기, SPM 구조로 재편
- [x] `NetworkProfile` 모델 (name, mode, ip, subnet, router, dns[], ssids[])
- [x] `networksetup -getinfo` 파싱 → 현재 구성·수동/DHCP 판별
- [x] `apply` 스크립트 (`-setmanual`/`-setdhcp`, 인자 화이트리스트 검증)
- [x] `config.json` 읽기·쓰기 + `config.example.json`
- [x] 설치 스크립트: `apply` 배치(root:wheel 0755) + sudoers(`visudo -c` 검증)
- [x] 제거 스크립트: sudoers·스크립트·설정·로그인 항목 **완전 원복** + 잔여물 검증
- [x] 파싱·상태 판별·인자 검증 단위 테스트 (`swift test` 69건 + 셸 검증 102건)

**Phase 1 결과물**

| 경로 | 역할 |
|---|---|
| `Package.swift` | SPM. `swift build` / `swift test` 로 Xcode 없이 빌드 |
| `Sources/WifiSwitcherCore/` | 모델·검증·파싱·설정. 프로세스를 띄우는 곳은 `SystemCommand.swift` 하나뿐 |
| `Sources/ExemWifiSwitcherCLI/` | `status` / `profiles` / `validate` / `apply` 확인용 CLI |
| `scripts/apply` | root 로 실행되는 유일한 스크립트. 인자는 프로필 이름 1개 |
| `scripts/install.sh` | 하는 일을 먼저 출력 → 확인 → 설치. `--dry-run` 지원 |
| `scripts/uninstall.sh` | 완전 원복 + 잔여물 0 검증. `--dry-run` / `--keep-config` |
| `Tests/shell/apply-tests.sh` | 인젝션 차단·설치 스크립트 검증. `swift test` 가 함께 실행 |

**인자 검증 규칙은 세 곳에 같은 내용이 있다** (다층 방어). 바꿀 때 함께 바꾼다.

1. `Sources/WifiSwitcherCore/ProfileName.swift` — 앱이 명령을 만들기 전
2. `scripts/apply` 의 `validate_profile_name` — root 로 실행된 직후
3. `/etc/sudoers.d/exem-wifi-switcher` 의 인자 패턴 — sudo 가 실행 자체를 거부
   (glob 은 문자 클래스의 반복을 표현할 수 없어 **길이별 고정 패턴 16줄**을 나열한다.
   `*` 를 쓰지 않으므로 공백·`;`·`/` 가 섞인 인자는 sudo 단계에서 걸러진다)

### Phase 2: 메뉴바 앱 ✅ 완료 (설치·등록은 미실행 — 사용자가 직접)

- [x] SPM 실행 파일 + `.app` 번들 조립 스크립트 (Info.plist·아이콘·ad-hoc 서명)
- [x] 메뉴바 아이콘으로 현재 상태 구분 표시 (`manual` / `dhcp` / `error` 템플릿 이미지, 없으면 SF Symbols)
- [x] 온보딩: 설치 **안내** — 앱은 sudo 를 대신 실행하지 않는다. 미설치 상태와 해결 방법을 메뉴·설정 창에 명시
      (위치 권한 요청은 SSID 를 실제로 읽는 Phase 3 로 미룬다. 권한 설명 키는 Info.plist 에 이미 있다)
- [x] **온보딩 분기** — 현재 수동이면 현재 값에서 프로필 추출 제안 / DHCP면 직접 입력 폼
- [x] 입력 검증 (IP·서브넷·라우터 형식, 라우터가 서브넷 안에 있는지) — `NetworkProfile.validate()` 재사용, 칸별 사유 표시
- [x] 메뉴에서 수동 전환 (전환 중·실패 상태 표시 포함)
- [ ] 자동 전환 on/off 토글 — **Phase 3 로 미룸.** 동작하지 않는 토글을 미리 두면 거짓말이 된다
- [x] 로그인 항목 등록 경로 (`.app` 번들만 등록, 설정 창에서 켜고 끔)
      — 실제 등록·시스템 설정 표시명 확인은 사용자가 직접 (에이전트는 시스템을 바꾸지 않는다)

**Phase 2 결과물**

| 경로 | 역할 |
|---|---|
| `scripts/build-app.sh` | SPM 빌드 → `.app` 조립(Info.plist·아이콘·ad-hoc 서명). `--print-plist` 는 빌드 없이 plist 만 출력 |
| `Sources/ExemWifiSwitcherApp/` | 메뉴바 앱. AppKit 글루만 있고 판단은 전부 코어에 있다 |
| `Sources/WifiSwitcherCore/StatusModel.swift` | 관측값 → 아이콘·머리말·전환 가능 여부. 시스템 호출 없음 |
| `Sources/WifiSwitcherCore/ProfileDraft.swift` | 온보딩 입력 다듬기·칸별 오류 배정·설정 조립 |
| `Sources/WifiSwitcherCore/LoginItem.swift` | 로그인 항목 상태·등록·해제. `SMAppService.mainApp` (2026-07-29 이전에는 LaunchAgent plist) |

**Phase 3 가 얹힐 자리**

- 상태 갱신: `StatusItemController.refresh()` — 지금은 30초 주기 + 메뉴 열 때. `SCDynamicStore` 감시가 들어오면 주기 확인은 보조로 물러난다
- 전환 실행: `StatusItemController.apply(profileName:userInitiated:)` — 자동 전환은 `userInitiated: false` 로 부르면 실패해도 창을 띄우지 않고 아이콘·메뉴에만 남는다
- SSID 목록: `NetworkProfile.ssids` 는 이미 있고 온보딩이 덮어쓰지 않는다 (`OnboardingSetup.makeConfig` 가 보존)

### Phase 3: SSID 자동 전환 ✅ 완료 (설치·권한 승인은 미실행 — 사용자가 직접)

- [x] CoreWLAN SSID 판독 + 위치 권한 미승인 시 안내 처리
- [x] `SCDynamicStore` 네트워크 변경 감시 (주기 확인은 60초 보조로 물러남)
- [x] 사내 SSID 목록(설정 가능, 기본값 제공) 매칭 → 프로필 적용
- [x] **무한 루프 방지** — 디바운스 + 현재 상태와 같으면 no-op + 정착 대기 + 실패 백오프 + 중단
- [x] 전환 시 알림 (모르는 사이 IP가 바뀌지 않도록)
- [x] 실패 시 폴백 — 전환 실패가 기존 연결을 망가뜨리지 않을 것
- [x] 자동 전환 on/off 토글 (Phase 2 에서 미룬 항목). 기본값 켜짐, `UserDefaults` 에 보관

**Phase 3 결과물**

| 경로 | 역할 |
|---|---|
| `Sources/WifiSwitcherCore/SSIDReading.swift` | SSID 판독 결과 6갈래. "못 읽었다" 를 이유별로 나눠 사용자에게 할 말을 남긴다 |
| `Sources/WifiSwitcherCore/AutoSwitch.swift` | **판정의 전부.** 관측값 + 지난 시도 기록 → 전환할지, 안 한다면 왜인지. 시스템 호출 없음 |
| `Sources/WifiSwitcherCore/AutoSwitchPreferences.swift` | on/off 보관 (기본 켜짐). 저장소를 주입할 수 있어 테스트가 시스템을 건드리지 않는다 |
| `Sources/WifiSwitcherCore/SwitchAnnouncement.swift` | 알림 문구. 사실만, 느낌표 없이 |
| `Sources/ExemWifiSwitcherApp/WiFiSSIDReader.swift` | CoreWLAN `CWInterface.ssid()` 를 부르는 유일한 자리 |
| `Sources/ExemWifiSwitcherApp/LocationAuthority.swift` | 위치 권한 요청·감시 (`CLLocationManager`) |
| `Sources/ExemWifiSwitcherApp/NetworkChangeMonitor.swift` | `SCDynamicStore` 감시 + 디바운스(조용해지면 1.2초, 최대 6초) |
| `Sources/ExemWifiSwitcherApp/SwitchNotifier.swift` | 알림 전달. 번들 밖이거나 권한이 없으면 조용히 물러난다 |
| `Sources/ExemWifiSwitcherApp/Diagnostics.swift` | `--diagnose` — 메뉴바가 안 보일 때의 상태 확인 통로. **주소 값은 찍지 않는다** |

**무한 루프를 막는 다섯 겹** (전부 `AutoSwitchPolicyTests` 가 지킨다)

1. **디바운스** — 한 번 갈아타는 동안 몰려오는 SCDynamicStore 이벤트를 하나로 묶는다
2. **no-op** — 현재 구성이 이미 목표면 명령을 내지 않는다. 전환이 이벤트를 낳고 그 이벤트가 다시 전환을 부르는 고리를 여기서 끊는다.
   **판정에는 IP·서브넷·라우터와 DNS 가 함께 들어간다** — IP 만 보면 사내 DNS 가 남은 채 밖에서 도는 구성을 정상으로 본다.
   다만 DNS 를 **읽지 못한 것**은 '다르다' 가 아니라 판정 불가다 (그렇게 다루면 조회 실패마다 무한 재적용이 된다)
3. **정착 대기** — 적용 직후 8초는 `-getinfo` 가 옛 값을 보여줄 수 있으므로 다시 걸지 않는다.
   시도했는데 결과가 기록되지 않은 경우도 같은 시간만큼 잡아 둔다
4. **헛도는 전환 감지** — 성공 이후 **한 번도** 목표 구성을 관측하지 못했으면 재적용을 멈추고 메뉴에 남긴다.
   반대로 한 번이라도 정착을 관측했다면 그 기록을 정산하고(`recordSettled`), 나중에 구성이 풀렸을 때 **다시 적용한다** —
   정산이 없으면 "적용은 됐었는데 나중에 풀린" 상황이 "효과가 없다" 로 굳어 그 Wi-Fi 를 떠날 때까지 멈춘다
5. **실패 백오프와 중단** — 10초 → 30초 → 90초 → 270초(상한 270초 — 5회째에 멈추므로 그보다 큰 상한은 쓰이지 않는다),
   5회 연속 실패하면 그 Wi-Fi 에서는 멈춘다

**멈춘 상태에서 빠져나오는 길이 Wi-Fi 변경 하나여서는 안 된다.** 자동 전환을 껐다 켜거나 메뉴의 **"지금 다시 시도"**
를 누르면 실패 기록을 지우고 그 자리에서 다시 건다 (원인을 고친 뒤 — 예: `install.sh` 재실행 — 앱을 다시 띄우지 않아도 되게).

**판단 근거가 없으면 아무것도 하지 않는다.** 위치 권한이 없거나 Wi-Fi 가 꺼져 있어 SSID 를 읽지 못하면 구성을 건드리지 않는다.
"모르면 DHCP" 로 두면 사내에서 인터넷이 끊긴다.

**사용자의 손이 자동보다 우선한다.** 메뉴에서 직접 고른 프로필은 그 Wi-Fi 에 머무는 동안 자동 전환이 되돌리지 않는다.
전환 중에 누른 프로필도 버리지 않고 지금 전환이 끝난 뒤 이어서 적용한다.

**관측은 한 번에 하나만 돈다.** 갱신을 부르는 문이 넷(주기 확인·SCDynamicStore·메뉴 열기·위치 권한 변경)인데
`probe.read` 는 수백 ms 가 걸린다. 겹쳐 돌면 늦게 시작한 읽기가 먼저 끝나 **낡은 관측이 새 값을 덮어쓰고**
그 값으로 전환을 판정한다. `StatusItemController` 가 요청을 하나로 묶어 직렬화한다.

**적용은 DNS 부터 한다.** IPv4 를 먼저 걸면 `-setdnsservers` 가 실패했을 때 'IP 는 새 값, DNS 는 옛 값' 인
절반짜리 구성이 남고, 그 상태는 IP 만 보면 목표와 같아 보인다. DNS 를 먼저 걸면 실패 시 아무것도 바꾸지 않은 채 멈추고,
IPv4 에서 실패하면 DNS 를 이전 값으로 되돌린다.

### Phase 3 미해결: 메뉴바 아이콘이 그려지지 않는 문제

Phase 2 에서 발견한 문제가 그대로 남아 있다. `NSStatusItem` 이 `isVisible=true` 이고 좌표까지 보고하는데 화면에 나타나지 않는다.

코드 쪽 흔한 원인은 전부 확인했고 **이상이 없다**.

| 점검 | 결과 |
|---|---|
| `NSStatusItem` 강한 참조 | `StatusItemController` → `AppDelegate` → 최상위 `delegate` 로 유지됨 |
| `setActivationPolicy(.accessory)` | `main.swift` 에 있음 |
| `LSUIElement` | Info.plist 에 `true` (테스트가 검사) |
| 아이콘 이미지 | 번들에 6개, 16×16 / 32×32. 크기 0 아님 |
| 첫 렌더 시점 | `applicationDidFinishLaunching` 이후 |

환경 요인이 유력하다 — **노치가 있는 MacBook Pro(Mac15,6) + 기존 상태 아이콘 12개 남짓**이면 새 항목이 노치 뒤로 밀려
그려지지 않는 알려진 동작에 부합한다. Phase 2 에서 표준 최소 예제도 같은 결과였다.
**확정하려면 다른 Mac(또는 외부 모니터)에서 확인해야 한다.** 여기서 더 파지 않는다.

대신 메뉴바가 유일한 출입구가 되지 않게 했다.

- `--diagnose` — 메뉴바 없이 상태 확인 (SSID 판독 여부·자동 전환 판정까지)
- 앱을 한 번 더 실행하면 이미 떠 있는 앱의 설정 창이 열린다 (`DistributedNotificationCenter`)
- CLI 로 상태 확인·수동 전환 (SSID 판독은 번들 앱에서만 가능하다)
- 아이콘 이미지를 못 찾으면 폭 0 항목이 되지 않도록 글자로 대체
- 사용자가 취할 수 있는 조치를 README "문제 해결" 에 기록

### 권한 점검 (설정 창 · `--diagnose` 공용) ✅ 완료

메뉴바 아이콘이 보이지 않는 환경에서는 설정 창이 사실상 주 출입구다. 그런데 권한 상태는
`--diagnose`(터미널)와 메뉴 몇 곳에 흩어져 있어, **무엇이 막혀서 안 되는지** 를 사용자가 조립해야 했다.
필요한 권한 넷을 설정 창 한자리에 모은다.

| 항목 | 확인 대상 | 갖춰지지 않았을 때 |
|---|---|---|
| 전환 권한 | `apply` 설치 + sudoers 규칙 파일 | 설치 명령 **복사** (앱은 sudo 를 대신 실행하지 않는다) |
| 설정 저장 권한 | `save-config` 설치 + 관리자 계정 | 미설치면 설치 명령 복사 / 비-admin 이면 **설치로 해결되지 않는다**고 알리고 버튼을 내밀지 않는다 |
| 위치 권한 | `CLAuthorizationStatus` | 시스템 설정 딥링크 |
| 알림 권한 | `UNAuthorizationStatus` | 시스템 설정 딥링크 |

| 경로 | 역할 |
|---|---|
| `Sources/WifiSwitcherCore/PermissionReport.swift` | **판정의 전부.** 관측값 → 항목별 상태·문구·조치. 시스템 호출 없음 |
| `Sources/WifiSwitcherCore/LocationAuthorizationState.swift` | 위치 권한 세 갈래. SSID 판독과 권한 점검이 함께 쓰므로 코어로 옮김 |
| `Sources/ExemWifiSwitcherApp/PermissionProbe.swift` | 시스템을 읽어 판정 입력을 만든다. **읽기만 한다** — 승인 창을 띄우지 않는다 |

- **판정은 한 곳에만 둔다.** 설정 창과 `--diagnose` 가 `PermissionReport` 를 나눠 쓴다.
  두 곳이 각자 판단하면 같은 시스템을 두고 다른 답을 내놓는 날이 온다
  (소스 대조 테스트가 진단 쪽에 권한 문구가 다시 생기는 것을 막는다)
- **창을 열 때와 앱이 활성화될 때마다 다시 읽는다.** 시스템 설정에서 허용하고 돌아온 사용자에게
  "거부됨" 을 계속 보여주면 고친 사람에게 안 고쳐졌다고 말하는 셈이다
- **아직 묻지 않은 권한은 문제로 세지 않는다.** 곧 승인 창이 뜰 상태를 경고로 칠하면 경고가 값싸진다
- 색은 **어긋난 항목의 상태 한 마디**에만 쓴다. 다 갖춰진 화면은 색이 없다
- 항목당 줄은 **상태 + 설명 한 줄**뿐이다. 문제가 없으면 왜 필요한지를, 있으면 무엇을 하면 되는지를 적는다
  (둘을 함께 쌓으면 네 항목이 여덟 줄이 된다)
- 로그인 항목은 권한이 아니라 설정이므로 **이 섹션에 넣지 않는다** — 기존 체크상자 그대로 두고 아래에 배치한다
- 설치 안내·저장 인증 안내가 창 아래쪽 안내 줄과 겹쳐서, 그 안내 줄은 **칸으로 좁힐 수 없는 입력 오류 전용**으로 물러났다

### Phase 4: 공개 배포 준비

- [x] `README.md` (한글) — 무엇을 하는 도구인지, 설치·제거, **설치되는 항목 전체 목록과 이유**, 문제 해결
      (Phase 3 에서 먼저 작성 — RULES.md §6 이 요구하는 투명성 문서다)
- [x] README 를 **다운로드 → 실행 → 앱에서 [설치]** 순서로 다시 씀. 터미널 방식은 "직접 빌드하기" 로 뒤에 남김
- [x] **Gatekeeper 안내** — 왜 막히는지(공증 없음)와 시스템 설정으로 여는 절차.
      `xattr -d com.apple.quarantine` 은 권하지 않는 방법으로 후순위에만 적음
- [x] `LICENSE` (MIT)
- [x] `.gitignore` — 빌드 산출물, 로컬 설정, `.DS_Store`
- [x] **실제 사내 IP·MAC 유출 여부 전수 점검** (RULES.md §3 — `git grep` 이 아니라 `grep -r`.
      추적되지 않은 새 파일을 빠뜨리지 않기 위해서다)
- [x] `scripts/install.sh` — 하는 일을 먼저 고지. 레포와 앱 번들 양쪽에서 실행된다
- [x] `scripts/uninstall.sh` — 완전 원복. 앱에서도 부를 수 있다
- [x] `scripts/package-release.sh` — 배포 zip + SHA-256 + 배포 전 점검 (RULES.md §4)
- [ ] `README.md` 스크린샷 (메뉴바 표시 문제가 풀린 뒤)
- [ ] 첫 릴리즈 게시 (앱 안 [설치]·[제거] 실동작 확인은 오너가 실기기에서 수행)
- [ ] GitHub Actions: 빌드 검증 (선택)
- [ ] macOS 업데이트로 설정이 풀렸을 때 복구 절차를 README에 기록

> **`docs/INSTALL.md` 는 만들지 않는다** (2026-07-28). 원래 계획에 있었으나 README 로 통합했다.
> 동료가 실제로 여는 문서는 README 하나이고, 설치 절차가 두 문서에 나뉘면 한쪽만 갱신되어 어긋난다.
> 설치가 "버튼 한 번" 으로 짧아져 별도 문서를 둘 만한 분량도 아니다.

**Phase 4 결과물** (앱 안 설치·제거)

| 경로 | 역할 |
|---|---|
| `Sources/WifiSwitcherCore/BundledInstaller.swift` | 인증 창 뒤로 넘어갈 **문자열**을 만든다. 경로·계정 이름 검사, `--dry-run` 인자, 터미널 폴백 명령. 시스템 호출 없음 |
| `Sources/ExemWifiSwitcherApp/InstallerService.swift` | 서명 확인 → `--dry-run` 으로 계획 받기 → 관리자 인증 실행. 설치 절차를 흉내 내지 않는다 |
| `scripts/package-release.sh` | 배포 zip + SHA-256 + 배포 전 점검 (RULES.md §4) |
| `scripts/build-app.sh` | 번들에 스크립트 다섯을 넣고 **그 뒤에** 서명한다 (내용이 서명에 봉인된다) |

**앱이 신뢰 경계가 아니라는 전제에서 하는 일** — `.app` 은 사용자가 쓸 수 있는 자리에 있으므로,
번들 안 스크립트를 고칠 수 있는 코드는 이 앱을 거치지 않고도 같은 일을 할 수 있다.
그래서 여기서 하는 것은 경계를 세우는 것이 아니라 **경계를 넓히지 않는 것**이다.

1. 넘기는 것은 `Bundle.main.bundlePath` 에서 조립한 경로 하나와 정해진 옵션뿐이다.
   파일 이름은 상수 둘 중 하나로 고정되고, 조립한 경로도 문자 화이트리스트로 다시 검사한다
2. root 로 오른 스크립트가 **자기 자리와 설치할 원본**이 남이 고칠 수 있는 상태인지 확인한다
   (`apply`·`save-config` 의 `assert_self_is_safe` 와 같은 발상)
3. 인증 창을 띄우기 전에 `codesign --verify --strict` 로 번들이 서명 뒤 바뀌었는지 본다.
   **ad-hoc 서명은 신뢰의 근거가 아니다** — 누구나 다시 서명할 수 있다. 손댄 흔적을 잡는 장치로만 쓴다.
   확인하지 못한 경우는 막지 않는다 (없는 근거로 설치를 불가능하게 만들지 않는다)
4. `visudo -c` 사전·사후 검증, `sudo -n -l` 확인, 상위 디렉터리 소유 검사는 그대로 남아 있다

## 진행 로그

| 날짜 | 내용 | 비고 |
|---|---|---|
| 2026-07-27 | 요구 수집·아키텍처 확정 | 초안: 위젯 + root 헬퍼 + XPC |
| 2026-07-27 | 위젯 철회 → 메뉴바 앱 | XPC·App Group·샌드박스 제약 소멸, sudoers 방식으로 단순화 |
| 2026-07-27 | 네이밍 확정 | 스파이크가 로그인 항목에 `server` 로 노출된 사고 반영 |
| 2026-07-27 | **Phase 0 완료 — SSID 판독 실증 성공** | `.app` 번들 + 위치 권한이면 읽힘. 자동 전환 전제 성립 |
| 2026-07-27 | **공개 배포로 방침 전환** | 소스 공개 + 각자 빌드. SPM 전환으로 Xcode 의존 제거 |
| 2026-07-27 | **Phase 1 완료 — 전환 코어** | SPM 재편, 검증·파싱·설정, `apply`/설치/제거 스크립트, 테스트 171건 |
| 2026-07-27 | 설치는 사용자가 직접 실행 | 에이전트는 시스템을 바꾸지 않는다 — 스크립트만 만들고 `--dry-run` 으로만 검증 |
| 2026-07-27 | **Phase 2 완료 — 메뉴바 앱** | `.app` 조립 스크립트, NSMenu, 온보딩(현재 구성 분기), 로그인 항목. 테스트 114건 |
| 2026-07-27 | 설정 저장은 **제자리 쓰기**로 | 파일을 갈아 끼우면 소유자가 사용자로 바뀌어 root 로 도는 `apply` 가 설정을 거부한다 (아래 참고) |
| 2026-07-27 | 자동 전환 토글을 메뉴에서 뺌 | 눌러도 아무 일이 없는 항목은 거짓말이 된다. Phase 3 에서 동작과 함께 넣는다 |
| 2026-07-27 | **Phase 3 완료 — SSID 자동 전환** | CoreWLAN 판독 + SCDynamicStore 감시 + 5겹 루프 방지 + 알림. 테스트 163건 |
| 2026-07-27 | SSID 를 못 읽으면 **아무것도 하지 않는다** | "모르면 DHCP" 는 사내에서 인터넷을 끊는다. 판단 근거가 없으면 현상 유지가 안전하다 |
| 2026-07-27 | 수동 선택이 자동보다 우선 | 자동이 사용자의 손을 곧바로 되돌리면 도구가 아니라 방해가 된다. Wi-Fi 가 바뀌면 자동으로 복귀 |
| 2026-07-27 | 메뉴바 아이콘 미표시 — 코드 이상 없음, 환경 요인으로 결론 | 노치 + 상태 항목 과다. 대신 `--diagnose`·재실행·CLI 로 출입구를 늘림 |
| 2026-07-27 | **설정 파일 권한 결정 뒤집힘** — `root:admin 0664` → `root:wheel 0644` | 기준선을 "admin이 apply를 부를 수 있다" 가 아니라 **설치 전 시스템**으로 잡아야 했다. 저장은 관리자 인증 1회(`save-config`), 전환은 그대로 무암호 |
| 2026-07-27 | **고정 IP 프로필에 DNS 필수** | 비우면 `apply` 가 시스템 DNS 를 지워 이름 해석이 끊긴다. 창의 안내 문구가 오히려 비우도록 유도하고 있었다. 세 계층(검증·초안·`apply`)에서 모두 막는다 |
| 2026-07-27 | DNS "읽지 못함" 과 "없음" 을 나눔 | 뭉개면 조회 실패 상태에서 빈 DNS 를 자기 값인 줄 알고 저장하게 된다 |
| 2026-07-27 | `apply` 가 설정 파일을 한 번만 읽도록 | 경로를 stat 한 뒤 plutil 로 수십 번 다시 여는 구조는 검사와 사용 사이가 벌어진다(TOCTOU). fd 를 잡고 `/dev/fd` 로 그 fd 를 검사한 뒤 사본에서만 파싱한다 |
| 2026-07-27 | 프로필 이름 규칙 **3계층 교차 테스트** 도입 | 패턴 개수만 세는 검사는 한 계층만 넓어진 드리프트를 못 잡는다. 같은 입력을 세 계층에 먹여 판정 일치를 대조한다 |
| 2026-07-27 | **정착을 관측하면 시도 기록을 정산한다** | 성공 기록이 지워지지 않아, 나중에 구성이 풀리면 '적용했는데 효과가 없다' 로 그 Wi-Fi 에서 영영 멈췄다. 사용자가 시스템 설정에서 IP 를 만지거나 macOS 업데이트가 되돌리면 곧바로 걸리는 길이다 |
| 2026-07-27 | **DNS 를 '이미 적용됨' 판정에 넣음** | 주석은 "DNS 는 따로 비교" 라고 적어 두었지만 그런 코드가 없었다. IP 만 맞으면 통과해, 사내 DNS 를 문 채 집에서 도는 구성이 정상으로 판정됐다. 읽지 못한 경우는 '다르다' 가 아니라 **판정 불가**로 다룬다 |
| 2026-07-27 | 적용 순서를 **DNS → IPv4** 로 뒤집고 되돌리기 추가 | 부분 적용이 남으면 IP 만 보고 '이미 적용됨' 으로 굳어 DNS 가 영영 고쳐지지 않았다. DNS 를 먼저 걸면 실패해도 손대기 전 그대로다 |
| 2026-07-27 | 멈춤에서 빠져나오는 손잡이 (`clearAttempts`) | 백오프·중단이 Wi-Fi 변경으로만 풀렸다. 사내에서 권한이 풀려 5회 실패한 뒤 `install.sh` 를 다시 실행해도 자동 전환은 멈춘 채였다. 토글 on · "지금 다시 시도" 로 연다 |
| 2026-07-27 | 관측 갱신을 **직렬화** | 갱신 진입점이 넷인데 in-flight 가드가 없어, 늦게 시작한 읽기가 먼저 끝나면 낡은 관측이 새 값을 덮어썼다. 최악은 Wi-Fi 를 갈아타는 순간 낡은 값으로 판정하는 것 |
| 2026-07-27 | 알림 권한 거부를 메뉴에 상시 표기 | 거부되면 전환이 **완전히 무성**이 된다. 메뉴바 아이콘 미표시와 겹치면 사용자가 알 통로가 없다. 승인 답을 기다리는 동안의 알림은 버리지 않고 들고 있다가 보낸다 |
| 2026-07-27 | 백오프 상한 300초 → 270초 | 5회째에 멈추므로 300초는 **도달 불가능한 숫자**였다. 문서와 코드가 갈라지는 자리를 없앤다 |
| 2026-07-27 | **설정 창에 권한 섹션** — 판정은 코어 한 곳(`PermissionReport`) | 권한 상태가 `--diagnose` 와 메뉴에 흩어져 있어 사용자가 조립해야 했다. 메뉴바가 보이지 않는 환경에서는 설정 창이 주 출입구다 |
| 2026-07-27 | sudoers 규칙 파일 유무를 따로 본다 | `apply` 만 있고 규칙이 없으면 **겉보기에 설치된 상태**인데 전환할 때마다 암호를 물어 실패한다. 두 상태를 다르게 말한다 |
| 2026-07-27 | 비-admin 계정에는 설치 명령을 내밀지 않는다 | 실행해도 상태가 달라지지 않는 명령을 조치라고 내놓으면 사용자를 헛돌게 한다 |
| 2026-07-28 | **배포·설치 방식 전환** — Releases 바이너리 + 앱 안에서 [설치] | "설치가 스크립트면 주변에 공유해도 거의 못 쓴다"(오너). 터미널을 여는 순간 대부분 포기한다. 설치 로직은 `install.sh` 한 곳에 그대로 두고, 앱이 그 파일을 번들에 품어 관리자 인증으로 실행한다 |
| 2026-07-28 | `install.sh` 가 원본을 **자기 위치 기준**으로 찾도록 | 레포 루트를 기준으로 삼으면 번들 안에서는 파일을 찾지 못한다. 같은 파일이 두 자리에서 같은 결과를 내야 두 경로가 갈라지지 않는다 |
| 2026-07-28 | `uninstall.sh` 에 `--user` 를 받게 함 | 관리자 인증을 거치면 root 라 HOME 이 `/var/root` 다. 그대로 두면 사용자의 로그인 항목·앱 설정값이 남았는데도 "전부 지웠다" 로 끝난다 |
| 2026-07-28 | 앱은 신뢰 경계가 아니라고 못박음 | `.app` 은 사용자가 쓸 수 있는 자리에 있다. 그래서 하는 일은 경계를 세우는 것이 아니라 **넓히지 않는 것** — 넘기는 경로를 우리가 만든 것 하나로 고정하고, root 로 오른 스크립트가 자기 자리를 확인하고, 서명으로 손댄 흔적을 잡는다 |
| 2026-07-28 | `docs/INSTALL.md` 를 만들지 않고 README 로 통합 | 동료가 실제로 여는 문서는 README 하나다. 설치 절차가 두 문서에 나뉘면 한쪽만 갱신되어 어긋난다 |
| 2026-07-28 | **메뉴에서 산문을 걷어냄** — 항목은 액션 아니면 짧은 명사구 | "여기 왜 이렇게 난잡하지?"(오너). 회색 설명이 세 줄이었고, 그중 한 문장이 메뉴 전체 폭을 정하고 있었다. 무엇이 액션이고 무엇이 상태인지 구분이 안 된다. 안내문은 **설정 창 권한 섹션에 이미 있는 중복**이었다 |
| 2026-07-28 | **한 번 더 정정 — 다 없애지 않고 짧게 남긴다** | 산문을 전부 걷어냈더니 이번에는 무슨 일이 일어나는지가 안 읽혔다. "아예 다 없애면 혼란이 오히려 생긴다"(오너). 보조 줄을 **보수적으로 골라** 되살리되 문장이 아니라 한 구(句)로 (`- 사내 IP 미등록` · `- 설정 창에서 설치` · `- 알림 꺼짐`) |
| 2026-07-28 | **위계는 색과 크기로 세운다** (`MenuStyle`) | 참조 메뉴에서 눈에 띈 것은 `라벨: 값` 이라는 형식이 아니라 **주 항목과 보조 문구의 색 대비**였다. 주 항목은 기본 라벨 색·기본 크기, 보조 줄은 `secondaryLabelColor` + 작은 글자 + `- ` 접두 + 한 단 들여쓰기. 색은 시스템 색만 쓴다 (회색을 박으면 라이트/다크 한쪽에서 깨진다) |
| 2026-07-28 | 머리말을 `isEnabled = false` 로 두지 않는다 | AppKit 은 비활성 항목을 통째로 흐리게 칠하고 **그 흐림이 `attributedTitle` 의 색을 덮는다**(실측: `labelColor` 를 줬는데 회색으로 나왔다). 그러면 메뉴에서 가장 중요한 한 마디가 딸린 보조 줄과 같은 밝기가 된다. 동작을 주지 않는 것으로 충분하다 |
| 2026-07-28 | 문제는 **상태 한 줄 + 액션 항목**으로 함께 낸다 | 조치만 있으면 무슨 일이 일어나는지 안 읽히고, 상태만 있으면 어디로 갈지 모른다. `- 알림 꺼짐` + `알림 설정 열기…` 처럼 짧게 짝지어 낸다 |
| 2026-07-28 | 머리말이 이미 말한 것은 토글 아래에 되풀이하지 않음 | `autoSwitchNote` 가 "설정을 마치면 자동으로 전환합니다" 처럼 머리말과 같은 말을 다시 했다. 판정 이유 자체는 `StatusModel.autoSwitchReason` 에 남겨 `--diagnose` 가 계속 쓴다 — 메뉴만 덜어낸다 |
| 2026-07-28 | 시스템 원문에 **한 줄 상한**(`StatusModel.lineLimit`) | 실패 메시지·설정 파일 오류는 길이를 고를 수 없다. 그대로 실으면 그 한 줄이 메뉴 폭을 정한다. 전문은 알림·실패 창·설정 창·`--diagnose` 가 들고 있다 |
| 2026-07-28 | `<앱 이름> 종료` → `종료` | macOS 관례지만 메뉴바 전용 앱에서는 **긴 제품명 하나로 메뉴 폭을 정하는** 값을 치른다. 어느 앱인지는 눌러서 연 아이콘이 말한다 |
| 2026-07-28 | **위치 권한 표시 모순 수정** — 관측된 사실이 아직 정해지지 않은 상태 값을 이긴다 | `--diagnose` 가 "위치 권한 아직 묻지 않음" 과 "Wi-Fi 이름 <읽힘>" 을 나란히 찍었다. `CLLocationManager` 는 만든 직후 '아직 묻지 않음' 을 주고 실제 값은 델리게이트로 오는데, 한 번 찍고 끝나는 경로에는 그 순간이 오지 않는다. **이름이 읽히면 권한은 있는 것**(`PermissionInput.wifiNameVisible`)이고, 증거가 없을 때만 잠깐 기다렸다 다시 본다(`LocationAuthority.settledState`) |
| 2026-07-28 | **잠갔다는 Wi-Fi 이름 칸이 실기에서 안 잠겨 있었다** — 판정을 창 여는 순간에만 했다 | 첫 실행에서는 창이 열리는 순간 위치 권한이 아직 없어 칸이 비고, 이름은 **권한을 허용한 뒤** `update(observation:)` 로 들어온다. 그 경로가 값만 채우고 잠금은 다시 정하지 않아, 자동으로 채워진 이름이 편집 가능한 채로 남았다. 판정을 코어(`SSIDFieldState`)로 옮겨 **값과 잠금을 함께** 정하고 이름이 들어오는 모든 경로에서 다시 부른다 (사람이 적고 있는 중에는 건드리지 않는다). 같은 자리에서 확인한 두 번째 사실 — **흐린 글자색만으로는 잠긴 칸이 편집 가능한 칸과 구분되지 않는다.** 잠기면 테두리를 지워 '적는 칸' 이 아니라 '적힌 값' 으로 보이게 한다 |
| 2026-07-28 | 잠긴 칸의 **설명 줄을 없애고**, 지운 테두리의 자리는 여백으로 채운다 | "세로 정렬 안 맞아. 밑에 description 부분은 없애도 돼"(오너). 모양(테두리 없음 + 흐린 글자)이 이미 잠겼다고 말하므로 같은 말을 한 줄 더 두지 않는다. 그리고 테두리를 지우면 칸이 6pt 낮아져 **그 줄만 낮아진다** — 테두리가 글자 둘레에 두던 여백을 가로·위·아래 모두 칸에게 물어 그대로 돌려주면, 잠긴 줄과 편집 가능한 줄이 픽셀 단위로 같은 자리를 쓴다 (위쪽은 글자가 앉는 높이로, 아래쪽은 남은 높이로 정한다) |
| 2026-07-28 | **사외에서 설치한 사람에게는 '채우라' 가 아니라 '언제 채워지는지' 를 말한다** | "회사 밖에서 설치한 사용자에 대한 설정 플로우도 고려"(오너). 사외 사용자는 사내 IP·서브넷·라우터를 알 길이 없고, 지금 붙어 있는 Wi-Fi 이름은 넣으면 안 되는 이름이다 — **빈 칸 다섯 앞에서 할 수 있는 일이 없다.** 그런데 사내에 가면 대개 이미 고정 IP 로 구성돼 있어 앱이 그 값을 읽어 넣는다. 그래서 설정 창 머리말과 메뉴 보조 줄을 그 사실로 바꾼다(`사내에서 열면 값이 채워짐` — 두 자리가 같은 낱말을 쓴다). 메뉴 보조 줄은 **값만 남았고 사외일 때만** 나온다: 권한이 함께 남았으면 그쪽이 먼저이고(사외에서도 지금 할 수 있다), 구성을 읽지 못했으면 어디 있는지 모르므로 짐작하지 않는다. 머리말은 `초기 설정하기` 그대로 둔다 — 문(門)이 사라지면 값을 아는 사람이 지금 넣을 자리도 사라진다 |
| 2026-07-28 | **저장하지 않은 값과 저장한 값이 화면에서 같아 보이던 문제** | "이게 다 설정됐는데 이건 그대로 유지돼"(오너 — 설정 창은 다 찼는데 메뉴는 '초기 설정하기 / 예시 설정 그대로'). **메뉴가 옳았다**: 설치된 `config.json` 에 `_readme` 가 남아 있었다(= `install.sh` 가 복사한 예시 그대로 = 저장한 적 없음). 갱신 누락이 아니다 — 저장 경로는 `onSaved` → `refreshAndEvaluate()` 로 곧바로 다시 읽는다. 진짜 문제는 **사내에서 창을 열면 지금 구성을 읽어 다섯 칸이 저절로 차는데 그 화면이 저장을 마친 화면과 똑같다**는 것. 머리말이 둘을 가르게 했다(`SettingsIntro` — 저장됨 / 아직 저장 안 됨 / 채울 값 없음 / 파일 읽기 실패). 메뉴 보조 줄 '예시 설정 그대로' 도 '**아직 저장 안 됨**' 으로 바꿨다 — 예시 파일은 설치 스크립트 쪽 사정이지 사용자가 아는 말이 아니다 |
| 2026-07-28 | **빈 칸에서는 저장을 막고, 사내에 붙는 순간 다섯 칸을 함께 채운다** | "빈칸에서는 저장을 못 누르게 하고 … 사내 WIFI가 연결된다면 자동으로 값이 들어가는 방식"(오너). 아직 아무것도 적지 않은 사람에게 칸마다 오류를 붙이는 것은 **틀린 값을 적었을 때 할 말**이다. IP·서브넷·라우터·DNS 가 다 차야 [저장] 이 눌린다(`hasRequiredValues` — Wi-Fi 이름은 없어도 프로필이 성립하므로 뺐다). 창을 열어 둔 채 사내에 붙으면 `update(observation:)` 이 **빈 칸만** 채우고(`adopting` — 손으로 적은 값은 덮지 않는다) 머리말과 저장 버튼이 함께 따라간다. 처음엔 '커서가 있는 칸은 건드리지 않는다' 로 막았다가, 창을 열면 IP 칸에 커서가 가 있어(`present`) **정작 그 칸만 안 채워지는 것**을 미리보기에서 잡았다 — 사람이 적어 둔 것은 커서가 아니라 **빈 칸인지 아닌지**가 지켜 준다 |
| 2026-07-28 | **저장을 놓치는 자리를 둘 다 막는다** — 설치 직후와 창을 닫을 때 | "처음에 권한을 다 켜주면 알아서 뭔가 바뀔 거라 생각했는데 저장을 꼭 눌러야 하더라"(오너). 권한을 갖추면 끝난 것처럼 느껴지고, 사내에서는 칸이 저절로 차 있어 더 그렇게 보인다. **설치 성공 알림**을 '설치했습니다 · 한 걸음 남았습니다' 로 바꾸고 기본 버튼이 그 자리에서 저장하게 했다. **창을 닫을 때** 저장하지 않은 값이 있으면 [저장하고 닫기]·[저장 안 함]·[계속 편집] 을 묻는다([취소]와 저장 직후는 묻지 않는다). **`install.sh` 로 설치와 저장을 한 번에 묶는 길은 택하지 않았다** — 그 스크립트는 root 로 돌며 `sudoers` 를 쓰는 자리다. 값을 그 길로 넘기면 (가) `save-config` 의 검증을 한 벌 더 두거나 (나) 검증 없이 root 가 사용자 파일을 설치 자리에 놓게 된다. 설정 파일의 값은 무암호 `apply` 를 통해 **그대로 root 의 `networksetup` 인자가 되므로**, 인증 한 번을 아끼자고 가장 위험한 스크립트의 계약을 넓힐 값이 아니다 (검증이 root 에서 한 번만 도는 지금 구조가 방어선이다) |
| 2026-07-28 | **정상 상태에는 보조 줄을 두지 않는다** — 지금 주소와 붙어 있는 Wi-Fi 이름을 걷어냄 | "이거 필요 없을 거 같으니 없애주고 … 여기서도 와이파이 이름 필요 없을 거 같으니"(오너). 근거도 함께 왔다 — 이 앱이 하는 일은 ① 사내·사외 전환을 쉽게 하는 것 ② 지금 어느 설정인지 확인하는 것이고, **어느 쪽인지는 메뉴바 아이콘과 프로필의 체크 표시가 이미 말한다.** 머리말 아래 `IP → 라우터` 와 자동 전환 아래 `Wi-Fi <이름>` 은 읽을 것만 늘리고 판단은 늘리지 않았다. **남기는 것은 문제와 남은 일뿐이다** — 전환 실패 사유 · 설정 파일 오류 · 아직 저장 안 됨 · `IP 주소 없음`(값이 아니라 어느 프로필도 서 있지 않다는 뜻) · Wi-Fi 꺼짐/미접속/위치 권한(지금 무엇에 붙어 있는지가 아니라 자동 전환이 성립하지 않는다는 뜻) · 알림 꺼짐. `autoSwitchReason` 자체는 그대로 뒀다 — `--diagnose` 는 폭이 아니라 사실이 중요해 전부 필요하다. 걷어낸 것은 **메뉴 쪽 필터**(`menuNotes`)다 |
| 2026-07-28 | **정상 상태에는 머리말도 두지 않는다** — 체크마크가 이미 말한다 | "`사내 고정 IP 적용 중` 이라는 메뉴가 아래 체크와 겹치는 것 같은데 굳이 있어야 하나?"(오너). 반박할 것이 없었다 — 정상 상태에서 그 줄은 **세 번째로 같은 말**이다(메뉴바 아이콘 · 머리말 · 프로필 체크마크). 바로 앞에서 세운 기준('정상 상태에는 보조 줄을 두지 않는다')을 머리말에도 그대로 적용한다. 가르는 잣대는 **'체크마크가 이미 말하고 있는가'** — 남은 일 없음 · 딸린 줄 없음 · 체크가 실제로 서 있음 · 전환 중 아님, **넷을 다 만족할 때만** 지운다(`MenuLayout.isToldByCheckmark`). 그래서 전환 중·전환 실패·설정 파일 오류·초기 설정하기·아직 저장 안 됨·프로필 없음은 그대로 남는다. 구분선은 무리 **사이에만** 넣는 규율이라 상태 무리가 서지 않으면 맨 위 구분선도 저절로 사라진다. 설정 창으로 가는 문은 `설정…`(⌘,)이 맡는다 — 머리말은 더 이상 그 문이 아니다 |
| 2026-07-29 | **[저장]은 '필수 칸이 다 참 + 저장된 값과 다름' 일 때만 눌린다** | "1. 모든 필수 field 가 채워졌을 때, 2. 열었을 때와 달리 변경점이 있을 때, 둘 다 성립할 때만"(오너). 전까지는 1번만 봐서 **저장 직후에도 버튼이 살아 있었다**. 견주는 기준은 '창을 열었을 때의 값' 이 아니라 **저장된 설정(`config.json`)** 이다 — 창을 열면 사내 구성을 읽어 빈 칸이 저절로 차는데 그 값은 아직 저장되지 않은 값이라 '바뀐 것' 으로 봐야 하고, 열었을 때를 기준으로 잡으면 자동으로 채워진 직후가 '바뀐 것 없음' 이 되어 저장을 놓치는 문제가 되살아난다. 저장된 것이 없으면 기준은 빈 값이고, 저장에 성공하면 방금 저장한 값이 곧바로 기준이 된다(파일을 다시 읽기 전까지). 견주기는 **뜻으로** 한다(앞뒤 공백·DNS 구분자 주변 공백은 같은 값 — `ManualProfileDraft.matches`). **네트워크 서비스(`AppConfig.service`)도 저장 대상**이라 함께 본다. **로그인 항목 체크상자는 제외** — 누르는 즉시 `~/Library/LaunchAgents` 에 반영되는 값이라 저장과 무관하다. 창을 닫을 때의 확인 시트도 같은 판정(`canSave`)을 쓴다 — 두 자리가 다른 기준을 쓰면 저장할 것이 없는데 '저장하지 않고 닫습니다' 를 묻게 된다 |
| 2026-07-29 | **누르는 즉시 반영되는 것은 반영됐다고 말한다** — 로그인 항목 체크상자 아래 확인 줄 | "로그인 시 자동 실행을 토글했는데 저장 버튼이 disabled 되니까 좀 이상하네"(오너). 이 체크상자는 누르는 즉시 `~/Library/LaunchAgents` 에 반영되는데(그래서 dirty 에서 뺐다) 바로 옆 [저장]이 비활성이라 **'이건 저장 안 해도 되나' 로 읽혔다.** 눌린 결과를 그 자리에서 말한다 — `✓ 로그인 항목에 등록됨` / `로그인 항목에서 제거됨`, 3초 뒤 스스로 사라진다. **자리는 늘 잡아 두고 글자만 들고 난다**(줄이 생겼다 없어지면 창 높이가 출렁인다 — 오류 줄과 같은 규율). 연달아 누르면 앞 타이머를 걷어낸다. **색은 표시(✓) 하나에만 얹는다** — 초록을 흐리게 하는 길을 재 봤더니(알파 80%·65%) 밝은 화면에서 읽기 어려워졌다(초록은 흰 바탕에서 이미 옅다). 색의 채도가 아니라 **양**을 줄였다. **실패는 지우지 않는다** — 못 봤는데 사라지면 켠 줄 알고 넘어간다. 체크를 되돌리고 빨간 한 줄로 알리며 원문은 툴팁에 남긴다(알림 창을 띄우던 것을 대신한다 — 되돌린 체크와 그 자리의 한 줄이면 놓칠 수 없다). 같은 성격의 자리를 훑었다: 메뉴의 자동 전환 토글은 체크 표시가 곧 상태이고 옆에 저장 버튼이 없어 그대로 두고, [명령 복사]는 이미 같은 방식('복사됨' 1.6초)이며, [설치]·[제거]·[위치 권한 허용]은 알림 창이나 권한 표가 결과를 말한다 |
| 2026-07-29 | **Phase 0 기록 정정 — 재빌드하면 위치 권한이 풀린다** | "재빌드·재서명해도 재승인 불필요" 로 적어 두었는데 실기에서 반대였다. ad-hoc 서명에는 고정된 신원이 없어 TCC 가 **코드 해시**로 앱을 가른다 — 새 빌드는 이름만 같은 다른 앱이다. 같은 뿌리에서 두 번째 일이 따라온다: 로그인 항목을 켜 둔 채 앱을 바꾸면 macOS 가 **"백그라운드에서 실행될 수 있습니다"** 알림을 한 번 띄운다. **둘을 한자리에 적었다**(README `업데이트할 때 생기는 일`) — 따로 적으면 우연히 겹친 일처럼 보이고, 그러면 업데이트한 사람이 앱을 의심한다. 알림 권한(번들 식별자)·전환 권한·설정 값(앱 밖 `/usr/local`)은 그대로 남는다는 것도 함께 적는다. 고칠 수 있는 성질이 아니므로(서명·공증은 비목표) 문서로 막는다 |
| 2026-07-29 | **자동 전환이 sudoers 규칙을 보지 않던 것** — 전환 권한 판정을 한 자리로 모음 | `AutoSwitchPolicy` 는 `apply` 파일 유무만 봤다. 그런데 전환은 `sudo -n` 으로 도므로 무암호 규칙이 함께 있어야 성립한다 — 규칙만 빠진 상태(macOS 업데이트가 `/etc/sudoers.d/` 를 정리하는 일이 있다)에서 **메뉴는 전환을 잠그는데**(`SetupChecklist` 는 셋을 다 본다) **자동 전환만 시도했다.** 결국 다섯 번 실패 후 중단으로 수렴하지만, 하지 않아도 될 시도 다섯 번과 그만큼의 실패 알림을 치렀다. 백오프·중단은 **모르는 실패**를 위한 장치지 결과가 뻔한 호출을 걸러 내는 자리가 아니다. 판정을 `SwitchingPermission`(apply + sudoers) 한 자리로 모으고 넷이 그것을 본다 — 체크리스트·메뉴의 `canSwitch`·설정 창의 권한 표·자동 전환. 홀드 이름도 뜻을 따라 넓혔다(`helperNotInstalled` → `switchingPermissionMissing`). **`save-config` 는 넣지 않았다** — 없으면 값을 저장할 수 없을 뿐 이미 저장된 설정으로 전환하는 것은 그대로 되고, 메뉴도 그 이유로는 잠그지 않는다. 갈라지지 않게 조합마다 대조한다(`SetupChecklistTests` — 판정을 한쪽만 되돌리면 깨진다). **권한이 없는 동안에는 시도가 없으므로 실패도 쌓이지 않는다** — 다시 설치하면 다음 판정에서 곧바로 되살아난다 |
| 2026-07-29 | **자동 전환이 켜져 있으면 프로필을 고를 수 없다** — 그리고 그 대가로 '수동 선택 우선' 을 걷어냄 | "자동 전환이 켜져 있으면 저 2개 옵션이 보이되 disabled 처리가 되면 좋겠어"(오너). 자동이 곧 되돌릴 선택을 눌리게 두는 것보다 잠그는 편이 정직하다. **감추지는 않는다** — 체크 표시는 머리말을 지운 뒤로 '지금 무엇이 서 있는가' 를 지고 있는 유일한 자리다. 잠금 사유는 더해진다(권한 없음·전환 중과 함께). 그런데 **켜짐만으로 잠그면 함정이 하나 생긴다** — 위치 권한이 없어 자동 전환이 서지 못하는 상태에서는 토글이 메뉴에 없어서, 프로필까지 잠그면 끄지도 못하고 고르지도 못한다. 그래서 '자동이 손을 쓸 수 있는가' (`MenuLayout.autoSwitchCanAct`) 를 함께 본다. 이 변경으로 **'수동 선택 우선'(`manualChoice`)이 도달 불가**가 되어 걷어냈다 — 부르는 자리가 눌린 메뉴 항목 하나뿐이고, 자동을 켜는 순간 기록이 거둬지며, 판정은 켜져 있을 때만 그 값을 본다. `canSwitch` 의 뜻이 '눌러서 고를 수 있는가' 로 좁아져, '전환 중인가' 를 묻던 자리는 `isSwitching` 을 따로 본다 (묶어 두면 자동 전환이 켜진 정상 상태에서 지워 둔 머리말이 되살아난다) |

| 2026-07-29 | **로그인 항목을 `SMAppService.mainApp` 으로 옮김** — 제약 3 이 틀렸다 | "켰는데 시스템 설정의 '로그인 시 열기' 에 안 보인다"(오너). **켜지긴 했다** — 다만 엉뚱한 목록에 있었다. macOS 는 `~/Library/LaunchAgents` 의 plist 를 `legacy agent` 로 분류해 **아래쪽 '백그라운드에서 허용'** 에 넣고, 사람이 찾는 곳은 **위쪽 '로그인 시 열기'** 다. 실측(BTM 로그): 우리 항목은 `name=EXEM Wifi Switcher, type=legacy agent, disposition=[enabled, allowed]` 로 기록돼 있었다 — 등록은 정상이고 다음 로그인에 뜬다. `launchctl print` 에 없던 것은 **plist 를 오늘 놓았고 그 뒤로 로그인한 적이 없기 때문**이다(launchd 는 로그인할 때 그 디렉터리를 읽는다). ── **그래서 제약 3 을 다시 쟀다.** `SMAppService` 가 Developer ID 를 요구한다는 판단은 **`.daemon`**(root 데몬) 기준이었고, 앱 자신을 올리는 **`.mainApp` 은 ad-hoc 서명으로 그냥 된다.** ad-hoc 번들로 `register()` 하니 `no error` · `status=enabled` 였고 BTM 은 `type=app, disposition=[enabled, allowed, not notified]` 로 기록했다 — **`app` 이라 '로그인 시 열기' 에 뜨고**, `not notified` 라 "백그라운드에서 실행될 수 있습니다" 알림도 없다. 코드 해시가 바뀌는 재빌드에도 등록이 풀리지 않았다(위치 권한과 다르다 — TCC 는 해시로 가르지만 BTM 은 그렇지 않다). ── **얻은 것 하나가 더 있다: 상태를 읽을 수 있다.** 옛 방식은 파일 유무만 알 수 있어 macOS 가 항목을 꺼도 체크상자는 켜진 채였다(README 에 "앱은 그 상태를 읽을 수 없다" 고 적어 두었던 그 자리다). `SMAppService.status` 는 `requiresApproval` 을 돌려주므로 **`blockedBySystem`** 상태를 따로 그린다 — 체크는 켠 채로 두고(사용자가 켠 것은 사실이다) 아래 줄로 "macOS 가 꺼 두었습니다" 를 **지우지 않고** 남긴다. ── **이관**: 옛 plist 가 있으면 앱이 뜰 때 걷어낸다(`migrateLegacyAgent`). 둘 다 두면 로그인할 때 두 벌이 뜬다. 켜 두었던 뜻은 이어받되(`off` 였을 때만 새로 켠다) **macOS 가 꺼 둔 것은 되살리지 않는다.** 번들 밖에서 실행 중(`unavailable`)이면 넘겨받을 수 없으므로 **옛 것도 지우지 않는다** — 지우면 사용자가 켜 둔 자동 실행만 사라진다. ── **대가**: 로그인 항목이 파일이 아니라 macOS 가 든 기록이 되어 **`uninstall.sh` 가 끄지 못한다**(셸용 공개 수단이 없다). 뭉개지 않고 스크립트가 그 자리에서 어떻게 끄는지 적고, 앱의 [제거]는 제거에 성공한 뒤 스스로 끈다. 앱 번들을 지우면 macOS 가 기록도 함께 정리한다(실측 확인) |

## 참고

- 환경: macOS 26.5.2, Xcode 16.4(macOS 15.5 SDK), Swift 6.1.2
- 실제 사내 네트워크 값은 **이 문서에 기록하지 않는다** (공개 레포)
- **설정 파일 소유권**: 설치된 `config.json` 은 `root:wheel 0644` 다. `apply` 는 root 소유가 아니거나
  root 외에 쓰기가 열린 설정을 거부한다(심링크·교체 공격 차단). 앱은 이 파일에 직접 쓰지 못한다 —
  검증을 통과한 내용을 사용자 전용 임시 파일(0700 디렉터리 / 0600 파일)에 놓고,
  `do shell script … with administrator privileges` 로 **관리자 인증을 한 번 받아** `save-config` 를
  root 로 실행한다. `save-config` 가 내용을 다시 확인하고 `install(1)` 로 갈아 끼운다.
  인증을 취소하거나 실패하면 이전 설정이 그대로 남고, 준비해 둔 임시 파일 경로를 그대로 알려
  터미널에서 이어서 저장할 수 있게 한다. 관리자 그룹이 아닌 계정에서는 **인증 창을 띄우기 전에** 알린다.
- Phase 0 부산물: 검증용 임시 앱에 부여된 위치 권한은 `tccutil reset Location <bundle-id>` 로 정리 가능
